all:
  children:
    proxmox_hosts:
      hosts:
        poseidon:
          ansible_host: 192.168.99.12
          ansible_user: alex
          ansible_ssh_private_key_file: /home/alex/.ssh/id_ansible
          ansible_ssh_common_args: '-o StrictHostKeyChecking=no'

